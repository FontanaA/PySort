import random

def rand_list(srart, stop, total):
    rand_list = [random.randint(0, 250) for i in range(20)]
    print(f"{rand_list} \nis the unsorted list")
    return(rand_list)

def bubble(x, gen):
    if gen == True:
        pass

